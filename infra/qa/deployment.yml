apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: ready-api-qa
  name: ready-api-qa
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ready-api-qa
  template:
    metadata:
      labels:
        app: ready-api-qa
    spec:
      containers:
        - image: ready-api
          name: ready-api
          ports:
            - containerPort: 80